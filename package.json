{
  "name": "root",
  "private": true,
  "npmClient": "yarn",
  "useWorkspaces": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": " ./scripts/clean.sh",
    "build": "lerna run build",
    "start": "lerna run start",
    "dev": "lerna run dev --parallel"
  },
  "devDependencies": {
    "lerna": "^3.21.0"
  },
  "dependencies": {}
}
